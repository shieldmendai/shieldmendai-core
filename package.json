{
  "name": "shieldmendai-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "ShieldMendAI Mobile Beta 0.1 Android debug build setup.",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:build:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
